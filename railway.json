{
  "deploy": {
    "startCommand": "npm run start --workspace=server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  },
  "build": {
    "buildCommand": "npm install && npm run build --workspace=client && npm run prisma:generate --workspace=server && npx prisma migrate deploy --schema=server/prisma/schema.prisma"
  }
}
