{
  "deploy": {
    "startCommand": "npm run start --workspace=server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  },
  "build": {
    "buildCommand": "npm install && npm run prisma:generate --workspace=server && npm run prisma:migrate:prod --workspace=server"
  }
}
